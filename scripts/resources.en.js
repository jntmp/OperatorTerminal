var resources = {
	en : {
		errors : {
			invalidLineNo:      "INVALID LINE NUMBER/FORMAT '{0}'",
			invalidOperator:    "INVALID OPERATOR '{0}'",
			invalidParams:      "INVALID/NO WORKING PARAMETERS",
            invalidSyntax:      "INVALID SYNTAX"
		},
		templ : {
			result : "{0}#:{1}={2}"
		}
	}
}